# Production build guard: API calls must stay relative (same-origin) so they
# go through the Cloudflare Pages function proxy to the Fly.io backend.
# This file outranks .env.local during `vite build`, so a local dev
# VITE_API_ORIGIN can never leak into a production bundle again.
VITE_API_ORIGIN=
